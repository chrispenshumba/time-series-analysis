from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project is a time series project on weather data at the Mother Cap point at the peak district.',
    author='chrispen',
    license='MIT',
)
